<div class="container-fluid py-3"><h3>Guardians</h3><p class="text-muted">Search by guardian name or phone number to see every linked child.</p><div class="card shadow-sm"><div class="card-body"><input wire:model.debounce.300ms="search" class="form-control mb-3" placeholder="Enter at least 2 characters of a name or phone number">@if(strlen(trim($search)) < 2)<div class="text-center text-muted py-4">Start typing to search guardians.</div>@else @forelse($guardians as $guardian)<div class="border rounded p-3 mb-3"><div class="d-flex flex-wrap justify-content-between"><div><h5 class="mb-1">{{ $guardian->name ?: 'Unnamed guardian' }}</h5><span class="text-muted"><i class="fas fa-phone mr-1"></i>{{ $guardian->phone }}</span></div><span class="badge badge-info align-self-start">{{ $guardian->students->count() }} children</span></div><div class="table-responsive mt-3"><table class="table table-sm mb-0"><thead><tr><th>Child</th><th>Admission no.</th><th>Current class</th><th>Status</th></tr></thead><tbody>@forelse($guardian->students as $student) @php($active=$student->sectionClassStudents->where('status','Active')->first()) <tr><td>{{ $student->name }}</td><td>{{ $student->admission_no ?: 'Pending' }}</td><td>{{ $active ? optional($active->sectionClass->section)->name.' / '.$active->sectionClass->name : 'Not assigned' }}</td><td>{{ $student->admission_status ?? ($active ? 'Admitted' : 'Pending') }}</td></tr>@empty<tr><td colspan="4" class="text-muted">No linked children.</td></tr>@endforelse</tbody></table></div></div>@empty<div class="text-center text-muted py-4">No guardians found.</div>@endforelse @endif</div></div></div>
